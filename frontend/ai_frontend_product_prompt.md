# 🔥 Prompt для AI-генерации полного фронтенда

# 🧠 **AI PROMPT: Product Frontend Generation Specification**

## 🎯 **Цель**
Сгенерировать полный фронтенд проекта, включая архитектуру, компоненты, страницы и UI-потоки, полностью соответствующие продуктовой концепции.
SPA-приложение на **React + TypeScript + Vite**, c Tailwind, но будь остарожен у него вышла новая спецификация v4 нужно использовать use context7 .
Использовать **shadcn-ui компоненты**, но стили заменять на собственные.

---
# 📦 **Общая структура проекта**
Приложение состоит из следующих блоков:
- Главная страница (MainPage, будет на "/" )
- Система загрузки файла (Drag & Drop)
- Loader с прогресс-баром
- Sidebar со списком карточек и фильтрами
- Список товаров (карточки)
- Бесконечный скролл
- Детальная карточка товара
- Управление вариантами товара (через Query Params)
- Модалку удаление карточки или удаления варианта карточки
- Страницу на создание карточки (но она находиться в двух местах и от этого немного измениться логика, если на странце с листом карточек, то он отправиться на бек для того чтобы он сам автоматически кинул в обработку и положил в нужную группу, а если уже пользователь вошел в группу то он добавит ее определенно в эту группу) 
- Редактирование варианта товара - прям в интерфейсе, тоесть все поля заменяються на импуты, и так же возможность добавить новые поля если каких то нету

Проект должен быть построен в модульной архитектуре со следующими директориями:

```
src/
  components/
    ui/
    file/
    sidebar/
    product/
  pages/
    MainPage.tsx
    ProductPage.tsx
  services/
    api.ts
    upload.ts
  types/
  hooks/
  lib/
```

---
# 🎨 **Визуальные требования (Design System)**
Используются следующие цвета (как CSS переменные):
```
--main-blue: #264B82;
--gray-blue: #D4DBE6;
--pale-blue: #E7EEF7;
--danger: #DB2B21;
--black: #1A1A1A;
--pale-black: #8C8C8C;
--green: #0D9B68;
--sea-dark: #167C85;
--sea-clear: #48B8C2;
--orange: #F67319;
--muted: #C9D1DF;
```
Использовать только эти токены.

Все shadcn компоненты должны быть стилизованы вручную.

---
# 🧩 **БЛОКИ ДЛЯ РЕАЛИЗАЦИИ**

## 1) **Начальный экран (без загруженных файлов)**
Отображается только:
- Большая зона Drag&Drop загрузки файла
- Текстовое описание
- Кнопка выбора файла
- Никакого sidebar

После успешной загрузки:
- Появляется sidebar
- Выполняется парсинг данных
- Переход к списку карточек

Компонент: `FileDrop.tsx`
Поведение:
- состояние dragover
- состояние обычное
- обработка onFile(file)

Если загруженные есть то сразу переходим к шагу "После успешной загрузки"

---
## 2) **Loader с прогрессом**
Компонент: `ProgressBar.tsx`
Состоит из:
- процентов
- текста
- кнопки отменить

Отображать во время обработки файла сервером.

---
## 3) **Sidebar**
Sidebar должен содержать:

- Фильтры (accordion)

- Список карточек (scrollable)
- На desktop всегда открыт
- На мобильных сворачиваться в иконку (через Sheet)

Компоненты:
- `Sidebar.tsx`
- `Filters.tsx`
- `SidebarItem.tsx`

---

## 4.1) **Над списком товаров**
- Поиск
- Кнопка "Создать карточку" (с автоматическим распределением, после создания перенаправит на продукты)

Компоненты:
- `SearchInput.tsx`
---
## 4) **Список товаров**
Компонент: `ProductList.tsx`
Функционал:
- выбор количества карточке на странице
- бесконечный скролл (так же сделать toggle в котором можно убрать режим автоподгрузки, и сделать кнопку "загрузить еще" при прокрутке в самый Списка товаров)
- skeleton при загрузке

Карточка товара (`ProductCard.tsx`) содержит:
- Фото
- Название
- Основные параметры
- ID
- Статус (цвет)
- Кнопки: редактировать, дублировать, удалить

---
## 5) **Страница товара**
Маршрут: `/product/:id`
Компонент: `ProductDetail.tsx`

Отображает:
- Полные данные карточки
- Список вариантов (size/color...) как табы/селекты, будет получаться и отображаться при выборе как отдельная карточка, со своей информацией 
- Подварианты управляются через query params
  Пример: `/product/123?card=45`
- Кнопки: редактировать, удалить, создать вариант
Для конкретики - :id это id группы однотипных карточек, а то на какой конкретно находиться пользователь это card=, он обязательный всегда, даже если карточка одна, потому что пользователь сам может создать еще карточку(вариант товара) в группу

---
## 6) 
**Модалки**
Модалки должны быть реализованы для:
- Удаления (с подтверждением)
- Вопрос: "Данные агрегированы?"

Использовать shadcn dialog + кастомные стили.

**Страницы**
- Создания карточки (в двух вариантах)

---
## 7) **API-слой**
Сервис `api.ts` должен экспортировать методы:
- getProducts(filters)
- getProduct(id)
- createProduct(data)
- updateProduct(id, data)
- deleteProduct(id)
- uploadFile(file, onProgress)

Сервис `upload.ts` — отдельный upload handler.

---
# 📌 **Требования к генерации кода**
1. Использовать React + TypeScript
2. Каждый компонент должен содержать:
   - описание props
   - состояние
   - события
3. Код должен быть максимально автономным
4. Цвета только из токенов
5. Элементы shadcn должны быть перезаписаны кастомными стилями
6. Всё должно быть совместимо с Vite
7. Использовать Tailwind 4 и use context7 for Tailwind 4 (Только актуальное API)
8. Не добавлять сторонние библиотеки без необходимости
9. Артефакты должны быть перезаписываемыми в IDE (идемпотентные)
10. Весь функционал должен быть адаптивным.

---
# 🚀 **Задача AI**
**Сгенерировать код для всего приложения, согласно описанной архитектуре, включая:**
- Компоненты
- Страницы
- Сервисы
- Стили
- Хуки
- Роуты

Придерживаться точной структуры:
```
src/components/ui/
src/components/file/
src/components/sidebar/
src/components/product/
src/pages/
src/services/
src/types/
src/hooks/
```

Выводить код частями, строго по структуре. Не пропускать файлы. Не менять архитектуру.


